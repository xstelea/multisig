{
  "private": true,
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "prepare": "husky",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check .",
    "lint": "oxlint",
    "deploy:app": "fly deploy --config fly.multisig-app.toml",
    "deploy:server": "fly deploy --config fly.multisig-server.toml"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "^0.35.0",
    "oxlint": "^1.50.0"
  }
}
